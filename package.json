{
  "name": "cricket-cup",
  "version": "1.0.0",
  "description": "Cricket Tournament Management System",
  "main": "backend/server.js",
  "scripts": {
    "start": "npm install --prefix backend && cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "heroku-postbuild": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [
    "cricket",
    "tournament",
    "management",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
